{
	"name": "imbit-pixels",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"dev": "vite --host",
		"zip": "npm run build && mkdir -p dist && zip -r dist/game.zip www -x \"**/.DS_Store\""
	},
	"dependencies": {
		"kaplay": "^3001.0.2"
	},
	"devDependencies": {
		"vite": "^6.1.1"
	}
}
