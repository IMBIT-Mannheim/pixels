{
	"name": "imbit-pixels",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"dev": "vite --host",
		"zip": "npm run build && mkdir -p dist && zip -r dist/game.zip www -x \"**/.DS_Store\"",
		"start": "node server.js"
	},
	"dependencies": {
		"kaplay": "^3001.0.17",
		"express": "^4.18.2"
	},
	"devDependencies": {
		"vite": "^7.1.1"

	}
}
